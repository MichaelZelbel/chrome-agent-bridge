{
  "name": "chrome-agent-bridge-mcp",
  "version": "0.4.0",
  "description": "MCP server exposing the Chrome Agent Bridge as agent tools over stdio. A thin proxy over the bridge's HTTP API — it does not modify the bridge, Chrome, or the PC-side gateway.",
  "type": "module",
  "main": "server.js",
  "bin": {
    "chrome-agent-bridge-mcp": "server.js"
  },
  "scripts": {
    "start": "node server.js"
  },
  "keywords": ["mcp", "model-context-protocol", "chrome", "browser-automation", "ai-agent"],
  "license": "MIT",
  "engines": { "node": ">=18" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
